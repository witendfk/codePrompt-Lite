# CodePrompt 功能 TODO

## ✅ 已完成

### 核心代码操作
- [x] **文件写入工具** - `write_file.js` 实现，支持自动创建目录
- [x] **文件读取工具** - `read_file.js` 实现，支持 offset/limit 分页
- [x] **Shell 执行工具** - `bash.js` 实现，Windows 用 PowerShell，其他用 Bash
- [x] **流式输出** - 已实现（ora spinner + 实时输出）

### 代码搜索
- [x] **Grep 搜索工具** - `grep.js` 实现，支持正则表达式和 glob 文件过滤
- [x] **Glob 文件搜索** - `glob.js` 实现，根据模式查找文件路径

### 代码理解
- [x] **规则匹配系统** - `.front.md` 规则自动匹配
- [x] **文件标签系统** - 支持 `@file` 引用文件内容

### 用户体验
- [x] **对话历史管理** - 跨会话持久化，退出时自动保存
- [x] **指令系统** - `/exit` 等指令处理
- [x] **用户交互工具** - `confirm.js`（yes/no）、`select.js`（选项列表）

### 集成
- [x] **MCP 工具集成** - 支持连接 MCP 服务器
- [x] **Skill 系统** - 支持 skill 加载

---

## 🔴 高优先级

### 核心代码操作
- [ ] **文件编辑工具**
  - 创建 `editFile` 工具，支持精确字符串替换
  - 添加修改前确认机制（diff 预览）
  - 支持批量文件操作

- [ ] **差异预览**
  - 显示文件修改前后的 diff
  - 使用颜色高亮变化（绿增/红删）
  - 支持分页查看大型 diff

- [ ] **Shell 安全增强**
  - 危险命令确认（rm、git reset --hard、> /dev 等）
  - 命令输出截断（防止输出过大）
  - 命令超时控制

### 代码搜索
- [x] ~~Grep 搜索工具~~ - 已完成
- [x] ~~文件搜索工具~~ - 已完成

## 🟡 中优先级

### 代码理解
- [ ] **项目结构分析增强**
  - 自动识别项目类型（Node.js、Python 等）
  - 提取关键配置文件（package.json、requirements.txt 等）
  - 识别框架和依赖

- [ ] **代码解析**
  - 创建 `parseFile` 工具，解析代码语法
  - 提取函数/类/变量定义
  - 提取导入/依赖关系
  - 支持 JavaScript/Python 等语言

### Git 集成
- [ ] **Git 工具集**
  - `gitDiff` - 查看 git 变更
  - `gitStatus` - 查看 git 状态
  - `gitCommit` - 生成并创建提交
  - `gitLog` - 查看提交历史

### 执行能力
- [ ] **代码运行**
  - 创建 `runCode` 工具，执行代码片段
  - 支持 JavaScript（Node.js）
  - 捕获执行结果和错误

- [ ] **测试执行**
  - 创建 `runTests` 工具，运行测试
  - 支持 npm test、pytest 等
  - 解析测试结果并汇总

### 用户体验
- [ ] **Token 统计**
  - 统计每次对话的 token 使用量
  - 显示预估成本
  - 生成使用报告

- [ ] **会话管理增强**
  - 查看历史会话列表
  - 恢复历史会话
  - 清除历史功能

## 🟢 低优先级

### 高级功能
- [ ] **多模型支持**
  - 支持切换不同的大模型
  - 支持同时使用多个模型
  - 模型性能对比

- [ ] **代码审查**
  - 审查 Pull Request
  - 生成审查意见
  - 支持本地 diff 审查

- [ ] **重构工具**
  - 自动重命名变量/函数
  - 提取函数/方法
  - 移动文件/目录

- [ ] **测试生成**
  - 根据代码生成单元测试
  - 支持多种测试框架
  - 生成边界条件测试

### 多模态
- [ ] **图片支持**
  - 发送图片给 AI
  - 图片理解（截图、UI 分析）
  - 生成图片描述

- [ ] **语音输入**
  - 语音转文字输入
  - 支持多语言

### 协作
- [ ] **会话分享**
  - 导出对话记录
  - 生成分享链接
  - Markdown 导出

- [ ] **配置同步**
  - 云端配置同步
  - 多设备配置共享

---

## 技术债务

### 🔧 优化项
- [ ] **MCP 并行连接优化**
  - 当前问题：串行连接多个 MCP 服务器，启动慢
  - 实现方案：并发控制 + 进度提示
    - 创建并发池（最多同时 3 个连接）
    - 显示连接进度：`连接 MCP... 2/5`
    - 单个连接超时控制（10 秒）
    - 连接失败不阻塞其他服务
  - 启动时显示：
    ```
    正在连接 MCP 服务器...
      ├─ AliyunBailianMCP ✓
      ├─ chrome-devtools ✓
      └─ fs-mcp (超时，跳过)
    ```

- [ ] **错误处理完善**
  - 统一错误处理机制
  - 添加详细日志
  - 友好的错误提示

### 📝 代码质量
- [ ] TypeScript 类型定义（功能完善后统一迁移）
- [ ] 单元测试覆盖
- [ ] 文档完善（README、API 文档）

---

## 更新日志

- 2025-01-11: 核心工具（bash、read_file、write_file、grep、glob）已完成
